# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'wordnik_ios' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for wordnik_ios
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  target 'wordnik_iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'wordnik_iosUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
